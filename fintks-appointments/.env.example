# Server Configuration
PORT=3000
NODE_ENV=development
# Frontend Configuration
VITE_API_URL=http://localhost:3000
# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# Email Configuration
# Using an app password instead of regular password for Gmail
# To generate an app password:
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Sign in with your Google account
# 3. Select "App" and choose "Other (Custom name)"
# 4. Enter a name like "Fintks Appointments"
# 5. Click "Generate"
# 6. Copy the 16-character password (no spaces)
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password

# Database Configuration
DB_PATH=appointments.db
#To make a user admin you need access to database and change the user role to `admin`
